# Normalise line endings so a contributor's platform never shows up as a diff.
* text=auto eol=lf

# Windows scripts keep CRLF.
*.ps1 text eol=crlf
*.bat text eol=crlf

# Binaries git must not touch.
*.png  binary
*.jpg  binary
*.apk  binary
*.idsig binary
*.keystore binary
*.jks  binary
